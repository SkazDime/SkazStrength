# SkazStrength
Skazius Strength Tracker is a lightweight, static SPA that runs entirely in the browser. Built with a small React + vanilla CSS stack, it provides an exercise library, a workout-logging form, lightweight analytics (SVG charts), and templates to speed data entry.
